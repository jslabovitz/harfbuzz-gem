hb_glyph_info_t *
get_glyph_info(VALUE obj);

hb_glyph_position_t *
get_glyph_position(VALUE obj);

hb_buffer_t *
get_buffer(VALUE obj);

hb_blob_t *
get_blob(VALUE obj);

hb_face_t *
get_face(VALUE obj);

hb_font_t *
get_font(VALUE obj);